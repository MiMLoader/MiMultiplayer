{
  "name": "mod",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "moon overlay:build && bun build ./src/index.ts --outdir ./out && cp ./mod.json ./out/mod.json"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@elysiajs/eden": "^1.2.0"
  }
}